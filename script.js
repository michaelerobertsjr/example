'use strict'

var names = [];
var keyName = 'first';

names.push({first: 'mike'});

console.log(names[0].first);
console.log(names[0][keyName]);

var myValue = (x == 'yes' ) ? true : false;

if(x == 'yes') {
  // not a lot of code
  return true;
} else {
  return false;
}
